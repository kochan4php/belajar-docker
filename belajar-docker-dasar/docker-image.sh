# Melihat semua docker image yang tersedia di docker daemon
sudo docker image ls

# Mendownload docker image dari docker registry
sudo docker image pull namaimage:tag

# Menghapus docker image dari docker daemon
sudo docker image rm namaimage:tag